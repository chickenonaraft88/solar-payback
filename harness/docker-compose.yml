services:
  ha-test:
    image: ghcr.io/home-assistant/home-assistant:2025.1.4
    container_name: solar-payback-ha-test
    ports:
      - "8123:8123"
    volumes:
      - ./config:/config
      - ./configuration.yaml:/config/configuration.yaml
      - ../custom_components/solar_payback:/config/custom_components/solar_payback
